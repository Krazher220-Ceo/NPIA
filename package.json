{
  "name": "npi-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash build.sh",
    "postinstall": "cd frontend && npm ci"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

