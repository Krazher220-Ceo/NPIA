{
  "name": "npi-platform",
  "version": "1.0.0",
  "description": "Национальная платформа промышленной аналитики Казахстана",
  "private": true,
  "scripts": {
    "build": "sh -c 'cd frontend && npm ci && npm run build'",
    "postinstall": "sh -c 'cd frontend && npm ci'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

