# 🏭 Национальная платформа промышленной аналитики Казахстана

Единая цифровая платформа для сбора, анализа и оптимизации производственных процессов на промышленных предприятиях Казахстана с использованием искусственного интеллекта и предиктивной аналитики.

---

## 📋 Описание проекта

### Что это такое?

**Национальная платформа промышленной аналитики** — это комплексная система для мониторинга, анализа и оптимизации работы промышленных предприятий. Платформа собирает данные с оборудования в реальном времени, анализирует их с помощью машинного обучения и предоставляет рекомендации по улучшению эффективности производства.

### Основные возможности

- 📊 **Мониторинг в реальном времени** — сбор данных с IoT-датчиков и интеграция с существующими системами
- 🤖 **Предиктивная аналитика** — предсказание отказов оборудования с точностью 85%+ с помощью ML
- 📈 **Расчет KPI и OEE** — автоматический расчет показателей эффективности оборудования
- 🔔 **Выявление аномалий** — автоматическое обнаружение отклонений и уведомления в реальном времени
- 💡 **Рекомендации по оптимизации** — персонализированные советы по улучшению процессов
- 📄 **Отчеты и аналитика** — автоматическая генерация отчетов и визуализация данных

### Целевые отрасли

- Металлургия и горнодобыча (АрселорМиттал, Казхром, ERG)
- Нефтегазовая отрасль (Тенгиз, Карачаганак, КМГ)
- Химическая промышленность (КазАзот, Атырауский НПЗ)
- Пищевая индустрия
- Машиностроение
- Цементная промышленность

---

## 🚀 Быстрый запуск

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
# Запустите скрипт быстрой настройки
./quick_start.sh
```

Скрипт автоматически:
- ✅ Проверит все зависимости
- ✅ Запустит Docker контейнеры (PostgreSQL + Redis)
- ✅ Настроит Backend (установит зависимости, применит миграции, заполнит тестовыми данными)
- ✅ Настроит Frontend (установит зависимости)

После выполнения скрипта запустите сервисы вручную (см. ниже).

### Вариант 2: Ручной запуск

#### 1. Проверка зависимостей

```bash
# Проверьте, что установлены:
python3 --version  # Должно быть 3.11+
node --version     # Должно быть 18+
docker --version
docker-compose --version

# Или используйте скрипт проверки:
./check_dependencies.sh
```

#### 2. Запуск базы данных

```bash
docker-compose up -d
```

#### 3. Настройка и запуск Backend

```bash
cd backend

# Создание виртуального окружения (если еще не создано)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt

# Применение миграций БД
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
alembic upgrade head

# Заполнение тестовыми данными (опционально)
python3 -c "import asyncio; from app.db.seed import main; asyncio.run(main())"

# Запуск сервера
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

Backend будет доступен на: **http://localhost:8000**

#### 4. Настройка и запуск Frontend

Откройте **новый терминал**:

```bash
cd frontend

# Установка зависимостей (если еще не установлены)
npm install

# Режим разработки
npm run dev
```

Frontend будет доступен на: **http://localhost:5173**

Или для production сборки:

```bash
npm run build
npm run preview  # Запустит на http://localhost:4173
```

---

## 🌐 Доступ к приложению

После запуска всех сервисов:

| Сервис | URL | Описание |
|--------|-----|----------|
| **Backend API** | http://localhost:8000 | REST API сервер |
| **API Документация** | http://localhost:8000/api/docs | Swagger UI с документацией API |
| **Frontend (dev)** | http://localhost:5173 | React приложение (режим разработки) |
| **Frontend (prod)** | http://localhost:4173 | Production сборка |
| **Лендинг** | http://localhost:5173/index_landing.html | Главная страница сайта |
| **Вход** | http://localhost:5173/login.html | Страница входа в систему |
| **Подача заявки** | http://localhost:5173/apply.html | Форма подачи заявки на роль руководителя |

---

## 🔑 Тестовые учетные данные

После заполнения БД тестовыми данными доступны следующие пользователи:

| Роль | Email | Пароль | Доступ |
|------|-------|--------|--------|
| **Администратор** | `admin@factory.kz` | `admin123` | Полный доступ ко всем функциям |
| **Менеджер** | `manager@arcelormittal.kz` | `manager123` | Управление заводом АрселорМиттал |
| **Инженер** | `engineer@anpz.kz` | `engineer123` | Управление оборудованием АНПЗ |

---

## 🏗️ Архитектура проекта

### Структура директорий

```
.
├── backend/                 # FastAPI Backend
│   ├── app/
│   │   ├── api/v1/         # API endpoints (REST)
│   │   │   └── endpoints/  # Конкретные эндпоинты
│   │   ├── core/           # Конфигурация, безопасность, БД
│   │   ├── models/         # SQLAlchemy модели (11 таблиц)
│   │   ├── db/             # Скрипты для заполнения БД
│   │   └── utils/          # Утилиты (PDF генерация и т.д.)
│   ├── alembic/            # Миграции базы данных
│   ├── requirements.txt   # Python зависимости
│   └── venv/              # Виртуальное окружение
│
├── frontend/                # React + TypeScript Frontend
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── services/      # API клиенты
│   │   └── hooks/         # React хуки
│   ├── public/            # Статические файлы
│   │   ├── index_landing.html  # Лендинг
│   │   ├── login.html          # Страница входа
│   │   └── apply.html          # Форма заявки
│   └── package.json       # Node.js зависимости
│
├── docker-compose.yml      # Конфигурация Docker (PostgreSQL + Redis)
├── quick_start.sh          # Скрипт быстрого запуска
└── README.md               # Этот файл
```

### Технологический стек

#### Backend
- **FastAPI** — современный веб-фреймворк для создания API
- **SQLAlchemy** — ORM для работы с PostgreSQL
- **PostgreSQL** — основная реляционная база данных
- **Redis** — кэширование и очереди задач
- **Alembic** — система миграций базы данных
- **Pydantic** — валидация данных и настройки
- **JWT** — аутентификация через токены
- **bcrypt** — хеширование паролей

#### Frontend
- **React 18+** — библиотека для создания пользовательских интерфейсов
- **TypeScript** — типизированный JavaScript
- **Vite** — быстрый сборщик и dev-сервер
- **Tailwind CSS** — utility-first CSS фреймворк
- **React Query** — управление серверным состоянием
- **Recharts** — библиотека для графиков и диаграмм
- **React Router** — маршрутизация
- **Axios** — HTTP клиент

#### Инфраструктура
- **Docker** — контейнеризация
- **Docker Compose** — оркестрация контейнеров

---

## 📊 Модель данных

Платформа использует **11 основных таблиц**:

1. **industries** — отрасли промышленности
2. **factories** — заводы/предприятия
3. **equipment_types** — типы оборудования
4. **equipment** — конкретные единицы оборудования
5. **metrics_catalog** — каталог метрик
6. **metrics_data** — данные метрик (хранятся в InfluxDB для временных рядов)
7. **users** — пользователи системы
8. **subscriptions** — подписки на тарифные планы
9. **applications** — заявки на роль руководителя
10. **production_cycles** — производственные циклы
11. **maintenance_log** — журнал обслуживания

Подробная схема БД описана в [PROJECT_DOCUMENTATION.md](PROJECT_DOCUMENTATION.md)

---

## 🔐 Безопасность

- ✅ **JWT аутентификация** — все API запросы защищены токенами
- ✅ **Хеширование паролей** — используется bcrypt с salt rounds ≥ 10
- ✅ **RBAC (Role-Based Access Control)** — контроль доступа на основе ролей
- ✅ **Multi-tenancy** — изоляция данных между заводами
- ✅ **Валидация данных** — все входные данные проверяются через Pydantic
- ✅ **HTTPS ready** — готовность к использованию SSL/TLS

---

## 📚 Документация

- **[PROJECT_DOCUMENTATION.md](PROJECT_DOCUMENTATION.md)** — Полная техническая документация
  - Детальная архитектура системы
  - Описание каждой библиотеки и её назначения
  - Модель данных и терминология
  - Планы развития и бизнес-модель

- **[SETUP.md](SETUP.md)** — Детальные инструкции по установке зависимостей

- **[QUICK_START.md](QUICK_START.md)** — Быстрый старт с тестовыми данными

- **[FEATURES_COMPLETE.md](FEATURES_COMPLETE.md)** — Список реализованных функций

---

## 🎯 Текущий статус проекта

### ✅ Реализовано

- Базовая архитектура (Backend + Frontend)
- 11 таблиц базы данных с миграциями
- REST API для всех основных сущностей
- JWT аутентификация и авторизация
- RBAC (контроль доступа на основе ролей)
- Multi-tenancy (изоляция данных по заводам)
- Лендинг с темной темой и переключением языков
- Отдельные страницы входа и подачи заявок
- Админ-панель для управления заявками
- Генерация PDF с учетными данными
- Дашборд с реальными данными из БД
- Заполнение БД тестовыми данными

### ⏳ В разработке

- ML модели для предиктивной аналитики
- Real-time обновления через WebSocket
- Интеграции с внешними системами (ERP, MES)
- Расширенная аналитика и отчеты
- Мобильное приложение

---

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
# Backend (с автоперезагрузкой)
cd backend
source venv/bin/activate
uvicorn app.main:app --reload

# Frontend (с hot reload)
cd frontend
npm run dev
```

### Создание миграций БД

```bash
cd backend
source venv/bin/activate
alembic revision --autogenerate -m "Описание изменений"
alembic upgrade head
```

### Заполнение БД тестовыми данными

```bash
cd backend
source venv/bin/activate
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
python3 -c "import asyncio; from app.db.seed import main; asyncio.run(main())"
```

---

## ☁️ Деплой на Vercel (временный фронтенд)

1. Установите Vercel CLI и авторизуйтесь:
   ```bash
   npm install -g vercel
   vercel login
   ```
2. В корне репозитория выполните:
   ```bash
   vercel --prod --confirm
   ```
   Конфигурация берётся из `vercel.json`: Vercel автоматически перейдёт в папку `frontend`, установит зависимости и соберёт Vite-приложение (`dist`) как статический сайт.
3. Добавьте переменную окружения `VITE_API_BASE_URL=https://<ваш-backend>` в настройках проекта на Vercel, если API будет вынесен отдельно.
4. После деплоя ссылка вида `https://<project>.vercel.app` готова к демонстрации. В шапке приложения сразу отображается уведомление о лицензии MIT.

> Backend (FastAPI) можно оставить локально или поднять на любой VPS. Для production-релиза рекомендуется подключить API через HTTPS и настроить CORS на домен Vercel.

## 🗂️ Работа с GitHub

Репозиторий [NPIA](https://github.com/Krazher220-Ceo/NPIA) сейчас пустой, поэтому:

```bash
git init
git add .
git commit -m "Initial release with MIT license and Vercel config"
git branch -M main
git remote add origin git@github.com:Krazher220-Ceo/NPIA.git
git push -u origin main
```

После первого push подключите стандартный workflow (например, GitHub Actions) или настройте автодеплой Vercel напрямую из репозитория.

## 📞 Контакты и поддержка

- **Email:** Krazher220@icloud.com
- **Телефон:** +7 (705) 669-76-77
- **Адрес:** г. Костанай

---

## 📄 Лицензия

Проект распространяется по лицензии **MIT** (см. файл [LICENSE](LICENSE)). На всех страницах интерфейса отображается уведомление о правовом статусе и ссылке на репозиторий.

---

## 🙏 Благодарности

Проект разработан для цифровизации промышленности Казахстана.

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025
./start_production.sh
./stop_production.sh