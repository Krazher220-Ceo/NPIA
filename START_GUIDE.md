# 🚀 Руководство по запуску проекта

## 📋 Быстрый старт (3 шага)

### Шаг 1: Автоматическая настройка

Запустите скрипт быстрого старта:

```bash
./quick_start.sh
```

Скрипт автоматически:
- ✅ Проверит все зависимости
- ✅ Запустит Docker контейнеры (PostgreSQL + Redis)
- ✅ Установит зависимости Backend и Frontend
- ✅ Применит миграции базы данных
- ✅ Заполнит БД тестовыми данными

---

### Шаг 2: Запуск Backend

Откройте **первый терминал** и выполните:

```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

Вы увидите:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

✅ Backend запущен на **http://localhost:8000**

---

### Шаг 3: Запуск Frontend

Откройте **второй терминал** и выполните:

```bash
cd frontend
npm run dev
```

Вы увидите:
```
  VITE v5.4.21  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

✅ Frontend запущен на **http://localhost:5173**

---

## 🌐 Доступ к приложению

После запуска обоих сервисов откройте в браузере:

| Что | URL | Описание |
|-----|-----|----------|
| 🏠 **Лендинг** | http://localhost:5173/index_landing.html | Главная страница |
| 🔐 **Вход** | http://localhost:5173/login.html | Страница входа |
| 📝 **Заявка** | http://localhost:5173/apply.html | Подача заявки |
| 📊 **Дашборд** | http://localhost:5173/ | После входа |
| 🔧 **API Docs** | http://localhost:8000/api/docs | Swagger документация |
| ⚙️ **Backend API** | http://localhost:8000 | REST API |

---

## 🔑 Вход в систему

Используйте тестовые учетные данные:

### Администратор (полный доступ)
```
Email: admin@factory.kz
Пароль: admin123
```

### Менеджер (управление заводом)
```
Email: manager@arcelormittal.kz
Пароль: manager123
```

### Инженер (управление оборудованием)
```
Email: engineer@anpz.kz
Пароль: engineer123
```

---

## 📦 Production сборка

Для production версии Frontend:

```bash
cd frontend
npm run build
npm run preview
```

Production версия будет доступна на **http://localhost:4173**

---

## 🛠️ Остановка сервисов

### Остановка Backend
В терминале с Backend нажмите: `Ctrl + C`

### Остановка Frontend
В терминале с Frontend нажмите: `Ctrl + C`

### Остановка Docker контейнеров
```bash
docker-compose down
```

---

## 🔄 Перезапуск проекта

Если нужно перезапустить всё с нуля:

```bash
# 1. Остановите все сервисы (Ctrl+C в терминалах)
# 2. Остановите Docker контейнеры
docker-compose down

# 3. Запустите скрипт быстрого старта
./quick_start.sh

# 4. Запустите Backend и Frontend (см. Шаги 2-3 выше)
```

---

## ❓ Решение проблем

### Проблема: "Port 8000 already in use"
**Решение:** Остановите процесс, использующий порт 8000:
```bash
lsof -ti:8000 | xargs kill -9
```

### Проблема: "Port 5173 already in use"
**Решение:** Остановите процесс, использующий порт 5173:
```bash
lsof -ti:5173 | xargs kill -9
```

### Проблема: "Docker не запущен"
**Решение:** Запустите Docker Desktop и подождите, пока он полностью загрузится.

### Проблема: "Module not found" в Backend
**Решение:** Переустановите зависимости:
```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
```

### Проблема: "Module not found" в Frontend
**Решение:** Переустановите зависимости:
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Database connection error"
**Решение:** Проверьте, что Docker контейнеры запущены:
```bash
docker-compose ps
# Если не запущены:
docker-compose up -d
```

---

## 📚 Дополнительная информация

- **Полная документация:** [PROJECT_DOCUMENTATION.md](PROJECT_DOCUMENTATION.md)
- **Описание проекта:** [README.md](README.md)
- **Детальная установка:** [SETUP.md](SETUP.md)

---

**Готово! Проект запущен и готов к работе! 🎉**

