# 🚀 Быстрый старт с тестовыми данными

## Шаг 1: Запуск базы данных

```bash
docker-compose up -d
```

## Шаг 2: Заполнение БД тестовыми данными

```bash
cd backend
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
python3 -c "import asyncio; from app.db.seed import main; asyncio.run(main())"
```

Или используйте скрипт:
```bash
cd backend
./run_seed.sh
```

## Шаг 3: Запуск Backend

```bash
cd backend
source venv/bin/activate  # или venv\Scripts\activate на Windows
uvicorn app.main:app --reload
```

Backend будет доступен на http://localhost:8000

## Шаг 4: Запуск Frontend

```bash
cd frontend
npm install  # если еще не установлены зависимости
npm run dev
```

Frontend будет доступен на http://localhost:5173

## Шаг 5: Вход в систему

Откройте http://localhost:5173 и войдите используя тестовые учетные данные:

### Тестовые пользователи:

1. **Администратор**
   - Email: `admin@factory.kz`
   - Пароль: `admin123`
   - Роль: admin (полный доступ)

2. **Менеджер**
   - Email: `manager@arcelormittal.kz`
   - Пароль: `manager123`
   - Роль: manager (управление заводом)

3. **Инженер**
   - Email: `engineer@anpz.kz`
   - Пароль: `engineer123`
   - Роль: engineer (управление оборудованием)

## Что доступно после входа:

✅ **Дашборд** с реальными данными:
- KPI карточки (OEE, активное оборудование, алерты, рекомендации)
- График OEE за последние 7 дней
- Статистика по заводам и оборудованию

✅ **Список заводов**:
- 3 тестовых завода
- Фильтрация и поиск

✅ **Оборудование**:
- 3 единицы оборудования
- Статусы и health scores

✅ **Аналитика**:
- KPI расчеты
- Аномалии
- Прогнозы
- Рекомендации

## API документация

После запуска backend, откройте:
http://localhost:8000/api/docs

Здесь вы можете:
- Просмотреть все доступные endpoints
- Протестировать API напрямую
- Увидеть схемы данных

## Структура тестовых данных

После выполнения seed скрипта в БД будет:

- **6 отраслей** промышленности
- **3 завода** (АрселорМиттал, Атырауский НПЗ, КазАзот)
- **4 типа оборудования**
- **3 единицы оборудования**
- **4 метрики** в каталоге
- **3 пользователя** (admin, manager, engineer)
- **KPI данные** за последние 7 дней
- **2 аномалии**
- **2 рекомендации**

## Решение проблем

### Ошибка "ModuleNotFoundError: No module named 'app'"

Убедитесь, что вы в директории backend и установили PYTHONPATH:
```bash
cd backend
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
```

### База данных не запущена

Проверьте статус:
```bash
docker-compose ps
```

Если контейнеры не запущены:
```bash
docker-compose up -d
```

### Frontend не подключается к Backend

Убедитесь, что:
1. Backend запущен на http://localhost:8000
2. В `vite.config.ts` настроен proxy на `/api`

## Следующие шаги

После успешного запуска вы можете:
1. Изучить код в `backend/app/` и `frontend/src/`
2. Добавить новые endpoints в API
3. Создать новые компоненты для frontend
4. Добавить реальные данные с оборудования

